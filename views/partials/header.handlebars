<header class="bg-gradient-to-tr to-cyan-700 from-cyan-900 flex justify-between items-center py-3 px-10">
    <h1 class="text-2xl font-extrabold">
        <span role="img" aria-label="Rocket">🚀</span>
        <a class=" text-white underline decoration-orange-500 decoration-4 underline-offset-2 font-extrabold" href="/">Cosmogeddon</a>
    </h1>
    <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="text-white text-lg" href="/profile">profile</a> <span class="text-white text-lg">|</span>
        <button class="no-button text-white text-lg" id="logout">logout</button>
        {{else}}
        <a class="font-bold text-md my-auto py-1 px-4 border-4 bg-cyan-50 border-orange-500 hover:border-orange-600 active:border-orange-700 rounded text-orange-500" href="/login">login</a>
        {{/if}}
    </nav>
</header>

