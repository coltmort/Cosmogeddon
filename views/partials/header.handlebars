<header class="bg-gradient-to-tr to-cyan-700 from-cyan-900 flex justify-between items-center py-3 px-10">
    <h1 class="text-2xl font-extrabold select-none">
        <span role="img" aria-label="Rocket">🚀</span>
        <a class=" text-white underline decoration-orange-500 decoration-4 underline-offset-2 font-extrabold select-none" href="/">Cosmogeddon</a>
    </h1>
    <nav class =" select-none">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="text-white text-lg select-none" href="/dashboard">dashboard</a> <span class="text-white text-lg select-none">|</span>
        <button class="no-button text-white text-lg select-none" id="logout">logout</button>
        {{else}}
        <a class="font-bold text-md my-auto py-1 px-4 border-4 bg-cyan-50 border-orange-500 hover:border-orange-600 active:border-orange-700 rounded text-orange-500 select-none" href="/login">login</a>
        {{/if}}
    </nav>
</header>

